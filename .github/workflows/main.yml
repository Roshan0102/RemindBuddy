name: Build and Deploy

on:
  push:
    branches: [ master, main ]

jobs:
  # Job 1: Build Android APK
  build-apk:
    name: Build Android APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'
          
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'
          
      - name: Get Dependencies
        working-directory: ./frontend
        run: flutter pub get
        
      - name: Build APK
        working-directory: ./frontend
        run: flutter build apk --release
        
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: frontend/build/app/outputs/flutter-apk/app-release.apk

  # Job 2: Deploy to Oracle VM (Backend + Web)
  deploy-server:
    name: Deploy to Oracle VM
    runs-on: ubuntu-latest
    needs: build-apk # Wait for build to ensure code is good
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USERNAME }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          script: |
            # Navigate to project directory
            cd /var/www/remindbuddy
            
            # Pull latest changes
            git pull origin master
            
            # Update Backend
            cd backend
            npm install
            pm2 restart remindbuddy-api
            
            # Build Web (Optional: if you want to build web on server)
            # Or you can build web in CI and transfer it. 
            # For now, we assume the server can build or we just sync code.
            # To be safe, let's just restart the server.
